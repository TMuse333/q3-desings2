import React, {useState} from "react";

interface Props {
    images:string[]
}

const Carousel:React.FC<Props> = ({images}) => {

    const [currentImage, setCurrentImageIndex]

    return (
        <section >

        </section>
    )
}